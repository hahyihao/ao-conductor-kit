# Worker Report: task-splitter sup16

## What changed

- Appended a new PM gate addendum to `experts/general/task-splitter.md` without altering any existing sections or numbering.
- Added seven mandatory checklist subsections to institutionalize the missing PM-layer controls identified by the sup §16 gap follow-up.
- Created this root report file as required for handoff.

## New section added

- `## 12. PM Gate Addendum (Superpowers alignment)`
- `### 12.1 Brief quality gate is mandatory`
- `### 12.2 Plan documents MUST NOT be worker dependencies`
- `### 12.3 Implementers default to sequential execution`
- `### 12.4 TDD is the default gate for testable changes`
- `### 12.5 Mini-spec self-review is required before code`
- `### 12.6 Gate function evidence bundle is required at handoff`
- `### 12.7 Re-review is mandatory after any substantive rework`

## Why this satisfies the request

- The expert file change is append-only and leaves all existing content, numbering, and order intact.
- The new addendum converts each requested gap into MUST / MUST NOT / REQUIRED policy language suitable for a P0 gate file.
- The content explicitly covers brief quality gates, the ban on plan-file dependency, implementer sequential execution, TDD defaulting, mini-spec self-review, evidence bundle requirements, and mandatory re-review after rework.
- No tracked files outside `experts/general/task-splitter.md` and this required root report were modified.

## PR link

- Pending PR creation
