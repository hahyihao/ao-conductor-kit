# FLOW.md

AO Conductor Kit 的核心不是“多开几个终端”，
而是把思考、编排、执行分给不同层级，
让最贵的认知资源停留在最该停留的地方。

这份文档定义一套严格的 CEO -> 项目经理 -> Worker doctrine。
它解释三层模型分别负责什么、
为什么要这样分层、
以及标准派发流程应该如何运行。

这不是实现文档，
也不是命令参考手册。
它是一份工作流 doctrine，
目的是让每次派发都保持一致、
可审计、
可恢复。

整套方法围绕一个现实展开：
用户当前的 Claude Code 交互窗口拥有很大的上下文，
但再大的上下文也仍然是有限认知带宽。
每一个 token 花在读文件、写代码、跑测试上，
就少一个 token 能花在理解用户意图、比较方案、写 brief、审核 PR 上。

因此最重要的原则只有一句：
战略留在 CEO，
执行交给下层。

## 核心原则

这套模型的第一原则是角色分离。
CEO 负责判断和授权，
项目经理负责翻译和编排，
Worker 负责在隔离环境中交付结果。
任何一层越界，
都会让效率和可追踪性一起下降。

这套模型的第二原则是上下文节流。
Claude 的大上下文窗口应该优先用于高杠杆动作：
理解用户需求、
发现隐含约束、
决定是否并行、
定义验收边界、
审核结果质量。
如果这些预算被实现细节和日志噪音占满，
CEO 就会逐步退化成一个高级执行员。

这套模型的第三原则是 issue 驱动。
一个子任务必须先成为一个清晰的 issue，
再绑定到一个 worker、
一个分支、
一个 worktree、
一个 PR。
这条链一旦稳定，
责任边界就自然稳定。

这套模型的第四原则是反馈回原链路。
CI 失败时不应把 CEO 拖回执行层，
review 评论也不应发给一个无关的新会话。
谁生成了该 PR，
后续修复优先回到谁。
这能减少上下文重建，
也能减少“别人来补锅却看不懂原意”的损耗。

这套模型的第五原则是只在值得的时候使用 AO。
AO 是杠杆，
不是仪式。
如果任务小到几分钟内就能在单人路径完成，
那就不应该为了流程感强行开 issue、spawn worker、等 PR。

## 第一层：CEO（总经理）

CEO 就是用户当前正在交互的 Claude Code 窗口。
它直接面对用户，
因此它的首要责任不是“做事”，
而是“把事情定义清楚并交给正确的人做”。

CEO 的角色可以概括为六个动作：

- 读取用户意图，识别真实目标、约束、优先级和隐含风险。
- 判断任务性质，是单点处理、单 worker 处理，还是适合拆成多个并行 issue。
- 写 brief，把模糊请求变成边界明确、验收具体的任务书。
- 派发任务，通过 AO 把工作交给 orchestrator 和 workers。
- 审核结果，对 PR 做质量审查，而不是重做执行。
- 对外汇报，用简洁文字向用户说明当前进度、风险和下一步建议。

CEO 的高价值在于判断，
不是在于“亲自下场”。
一旦 CEO 开始亲自读大量源文件、
跑构建、
调测试、
改实现，
它就会消耗本应用于战略和审核的认知预算。

因此 CEO 有非常明确的禁区：

- 不直接写代码。
- 不为了执行目的去读实现文件。
- 不跑测试、不跑构建、不做本地调试。
- 不把自己变成一个临时 worker。

CEO 可以使用终端，
但终端只应服务于 AO 编排和追踪。
常见允许动作包括：

- `ao status`
- `ao send`
- `gh issue create`
- `ao batch-spawn`

如果一个动作的主要目的是为了推进执行细节，
那通常就不属于 CEO。
CEO 最好的工作状态是：
上下文干净，
对全局有感，
能迅速给出下一步判断，
并且始终保留足够 token 预算做最终审核。

建议把 CEO 单次会话的 token 消耗控制在较低水平，
经验上尽量压在 80k 以内。
这不是硬阈值，
而是管理习惯。
一旦明显超标，
通常说明你已经把太多执行工作留在了 CEO 层。

CEO 的输出风格也应该符合总经理角色。
它给用户的不是原始日志，
而是经过压缩后的状态结论：
谁在做什么、
有哪些 PR 已经可以看、
哪些风险还在处理中、
下一步建议是什么。
CEO 说话要短，
但判断要硬。

## 第二层：项目经理（Project Manager）

项目经理层是 AO orchestrator session。
它的物理形态通常是一个运行在 WSL tmux 窗口中的 Codex 或 Claude Code CLI，
并由 AO 持续管理。

它不是一次性执行器，
而是一个常驻项目会话。
同一个 orchestrator 会在多个任务之间持续接收 CEO 的消息，
因此它承担的是项目级执行编排，而不是单次 issue 处理。

项目经理层的职责主要有五项：

- 接收 CEO 的高层意图和 brief。
- 把 brief 翻译成可稳定分配给 worker 的任务。
- 生成、追踪、唤醒和管理 workers。
- 维护 issue、worker、worktree、branch、PR 之间的映射关系。
- 监控执行流，确保异常能回到原 worker。

项目经理层不直接面对最终用户。
它面对的是 CEO。
因此它的目标不是重新解释需求，
而是让需求能够被稳定执行。

orchestrator 需要长期存在，
通常位于类似 `/root/.worktrees/ao-demo/demo-orchestrator-N` 的目录中，
并绑定自己的 `orchestrator/...` 分支，
从而不污染主开发分支和各个 feature 分支。

它的行为由 `agent-orchestrator.yaml` 的 `defaults.orchestrator` 配置约束。
这意味着 orchestrator 不是“靠记忆临场表演”，
而是一层带有明确工程边界的常驻系统组件。

一个项目通常只保留一个 orchestrator。
命名应稳定，
例如 `<sessionPrefix>-orchestrator-<N>`。
稳定命名带来的好处不是美观，
而是每次派发都知道消息应该送到哪里，
谁在维护这个项目的编排状态，
以及哪个常驻 session 应该接后续指令。

项目经理层最常见的失真有两种。
第一种是越级做 CEO 的事情，
比如自己重新定义需求战略，
而不是执行 CEO 已经定好的路线。
第二种是越级做 worker 的事情，
比如直接进入代码库改实现。
前者会造成战略漂移，
后者会破坏隔离和责任链。

项目经理层真正的产出不是代码，
而是有秩序的执行流。
如果它把工单发对了、
把状态追对了、
把异常送回原 worker 了，
它就完成了自己的价值。

但“常驻”不等于“上下文无限”。
当 orchestrator / PM 的上下文预算接近大约 `85%` 时，
它必须把这当成强制黄线，
先暂停继续派发，
写一份简短的 state summary，
再决定是 handoff 还是继续。
这份 summary 至少要覆盖：
当前目标、
当前 mode 和计划文档、
已完成项、
在途 issue / PR / session、
剩余 pending、
风险阻塞、
以及下一步最安全动作。

如果已经出现 compaction、
明显失忆、
或者需要靠“按之前讨论”才能继续，
这就不再是黄灯，
而是必须先交接的红灯。
继续硬撑的后果通常不是立刻崩溃，
而是状态漂移和静默降级。

## 第三层：Workers（员工）

Worker 是由 AO 按 issue 生成的 Codex CLI 会话。
每个 worker 都运行在独立 tmux 窗口里，
拥有自己的 Git worktree 和自己的 feature branch。

Worker 的基本原则只有一条：
一个 worker 只处理一个 issue。

这个“只”非常关键。
它确保 issue、分支、工作目录、PR、责任人之间形成一一对应关系。
一旦一个 worker 同时吃下多个 issue，
回溯就会变脏，
review 也会变脏，
后续回派更会变脏。

一个标准 worker 的生命周期很简单：

1. 读取 issue 及其上下文。
2. 在自己的 worktree 中完成实现或文档编写。
3. 创建清晰的 commit。
4. 推送自己的分支。
5. 打开一个指向 `main` 的 PR。
6. 在第一次 PR 创建后退出。

之后如果 CI 失败、
review 有意见、
issue 被补充说明，
再由 reaction engine 把它重新唤醒。

这种设计看上去像“短命会话”，
但它实际上是一种强隔离机制。
worker 不是持续在线的通用助理，
而是一个对单个 issue 负责到底的执行单元。

worker 的隔离体现为三点：

- 代码隔离：每个 worker 在自己的 `.worktrees/<prefix>-<N>` 下工作。
- 认知隔离：每个 worker 只需要理解自己的 issue，而不需要理解整个项目的所有任务。
- 责任隔离：出了问题时可以精确定位到哪一个 worker、哪一个分支、哪一个 PR 出了偏差。

典型并行启动方式是：
`ao batch-spawn 1 2 3 4 5`

这条命令的意义不是“开五个窗口聊五次天”，
而是“一次启动五条独立生产线”。
每条生产线只对自己负责，
最终都把结果推回 GitHub 形成可审查交付。

worker 的完成定义也必须严格。
不是“本地看起来差不多”，
不是“我已经做了八成”，
而是：
commit 已提交，
分支已推送，
PR 已创建，
并且 issue 到 PR 的映射是清楚的。
没有 PR，
就没有真正交付。

## 反应引擎

AO 的价值不止在于把任务派出去，
还在于它能处理派出去之后发生的大部分标准事件。
这部分由 lifecycle worker 完成，
它在后台持续轮询 GitHub。

lifecycle worker 主要监控三类事件：

- 打开中的 PR 的 CI 状态。
- 打开中的 PR 的新 review comments。
- 关联 issue 是否被人工关闭。

一旦发现变化，
它不会把问题甩回 CEO 让 CEO 重新下场。
它会优先通过 `ao send` 把信息送回最初负责该 issue 的 worker。
这样做的好处是责任连续、
上下文连续、
工作目录连续。

如果 CI 失败，
原 worker 收到失败详情后继续修。
如果 reviewer 提了意见，
原 worker 收到评论后继续改。
如果 issue 被人工关闭，
这条执行链就可以停止继续追踪。

这里还要加一层明确的 retry-loop breaker：

- 一个 worker session 最多只允许 8 个 turns，包含初始 brief 和后续 lifecycle 回派。
- 同一个 failure pattern 如果累计出现到第 3 次，就不再继续唤醒原 worker。
- 这时必须 kill 原 worker，respawn 一个 fresh worker，并把失败上下文注入新的 brief / prompt，让 replacement worker 不是 blind restart。

reaction engine 的存在让 CEO 不必盯着每一个失败日志。
正常情况下 CEO 只需要在节点处介入：
决定方向、
审查 PR、
向用户汇报结果。
只有当最大重试次数耗尽、
或者问题明显已经上升为 brief 错误、需求错误、架构错误时，
CEO 才需要升级介入。

## 标准派发流程

下面是 CEO 的标准 dispatch procedure。
这十步最好始终保持固定，
因为固定流程能显著减少遗漏和角色漂移。

1. 用户先用自然语言给出目标。
2. CEO 判断这是一个任务还是多个可并行任务。
3. 如果适合并行，CEO 为每个子任务写一份 brief。
4. 每份 brief 放在 `briefs/` 下，并且只服务一个 issue。
5. CEO 用 `gh issue create --body-file` 创建 N 个 GitHub issues。
6. CEO 用 `ao status` 检查 AO 是否已经运行。
7. 如果 AO 未运行，进入项目目录执行 `ao start`。
8. CEO 用 `ao batch-spawn <id1> <id2> <id3> ...` 生成 workers。
9. CEO 通过 `ao status` 或 dashboard 观察状态，等待 workers 交 PR。
10. PR 到来后由 CEO 审核，CI 或 review 异常则由 reaction engine 回派给原 worker。

这里要额外记一个操作纪律：
`ao status` 里的 `(unknown)` 本身既不能证明 session 健康，也不能证明它已经坏掉。
遇到 `(unknown)` 时，应把它当成需要继续核实的黄灯，
回到本仓库已经在用的观察路径交叉确认，
例如 dashboard、session activity、PR/CI 状态，
必要时再补看 `ao session ls -a` 或具体 worker 现场。

这个流程的重点不在于命令本身，
而在于 CEO 只在节点出现，
不在流水线上久留。
节点属于判断、授权、复核、汇报。
流水线属于实现、调试、修 CI、响应 review。
二者混在一起时，
这套模型就会迅速塌陷。

CEO 在派发阶段常用的 shell 命令通常只需要下面这些：

```bash
ao status
ao send <orchestrator-session> "<high-level intent>"
gh issue create --title "<title>" --body-file briefs/<brief>.md
ao batch-spawn <issue1> <issue2> <issue3>
```

如果 CEO 在频繁执行这些命令以外的大量开发命令，
一般就说明你已经把执行层工作留在了上层。

## ASCII 流程图

下面这张图描述用户、CEO、orchestrator、workers、GitHub 与 lifecycle worker 的关系。
图里最重要的不是“谁连着谁”，
而是反馈必须沿着原链路回去。

```text
User
  |
  v
+---------------------------+
| CEO / Claude Code Window  |
| Think / Brief / Review    |
+---------------------------+
              |
              v
+---------------------------+
| AO Orchestrator Session   |
| Project Manager           |
+---------------------------+
      |            |            |
      v            v            v
+-----------+  +-----------+  +-----------+
| Worker #1 |  | Worker #2 |  | Worker #3 |
| Codex CLI |  | Codex CLI |  | Codex CLI |
+-----------+  +-----------+  +-----------+
      |            |            |
      v            v            v
+-----------+  +-----------+  +-----------+
| worktree 1|  | worktree 2|  | worktree 3|
+-----------+  +-----------+  +-----------+
      \            |            /
       \           |           /
        \          |          /
         v         v         v
          +------------------+
          |      GitHub      |
          |   Push / PR / CI |
          +------------------+
                    |
                    v
          +------------------+
          | Lifecycle Worker |
          | Polling Engine   |
          +------------------+
                    |
        +-----------+-----------+
        |           |           |
        v           v           v
    Worker #1   Worker #2   Worker #3
```

这张图说明了三件事。
第一，用户只和 CEO 交互。
第二，worker 通过 GitHub 交付，而不是直接向用户汇报。
第三，CI 和 review 的反馈优先回到原 worker，而不是回到 CEO 亲自修。

## 何时使用 AO

AO 适合在“组织结构会直接影响交付质量”的任务里使用。
如果不分层就会互相干扰、上下文爆炸、难以回溯，
那通常就值得走 AO。

以下场景通常适合使用 AO：

- 任务跨多个文件，而且不同部分可以相对独立并行推进。
- 任务很深，需要长时间分析或重构，CEO 如果亲自下场会迅速耗尽上下文预算。
- 任务适合多角度并发分析，例如安全审计、性能审计、风格审计同时推进。
- 任务需要结果可审计、可复盘、可归责，而 issue -> branch -> PR 的链路正好能提供这种可见性。

可以把 AO 理解为一种放大器。
当单人直做会把认知和执行搅在一起时，
AO 能把它们重新拆开，
让高杠杆判断和低层执行分别停在合适的位置。

## 何时跳过 AO

不是所有任务都值得派发。
派发本身有成本：
写 brief、开 issue、生成 worker、等待 PR、做审核，
这些动作只有在任务复杂度足够高时才值得。

以下场景通常应跳过 AO：

- 一行修改或几分钟内可完成的小 bug fix。
- 需要实时 UI 反馈的前端调试，这类任务往往更依赖连续观察和即时试错。
- 仍在与用户高频对话、需求尚未稳定的探索型任务。
- 预计单 worker 15 分钟内就能完成的轻量工作。

一句话概括：
如果任务的主要矛盾不是执行规模，
而是即时沟通或极短路径试错，
就不要为了显得“体系化”而硬上 AO。
AO 是杠杆，不是仪式。

## 示例推演

假设用户说：“帮我审计交易策略的三个风险点。”

这时 CEO 的第一反应不应该是自己打开一堆策略文件开始读，
也不应该立刻写长篇分析。
正确动作是先判断：
这件事是否天然适合按风险维度拆分，
并把审计工作并行下发。

在大多数情况下，答案是适合。
因为“交易策略的三个风险点”通常可以拆成三个相对独立的审计方向，
例如：
仓位和风险暴露、
数据质量和回测输入、
执行路径和成交假设。

CEO 层会先做两件事。
第一，确认用户要的是“最高优先级的三个风险”，
还是“全量审计中的三个代表性风险”。
第二，决定按维度拆成三个 issues，而不是让一个 worker 全包。

接着 CEO 写三份 brief。

- Brief A：审计仓位管理、杠杆控制、风险暴露上限及止损机制。
- Brief B：审计行情数据、特征构造、标签生成、回测输入中的数据质量问题。
- Brief C：审计下单链路、滑点假设、撮合延迟、回测和实盘偏差。

然后 CEO 创建三个 GitHub issues，
检查 `ao status`，
必要时先执行 `ao start`，
再使用下面的命令并行派发：

```bash
ao batch-spawn <issueA> <issueB> <issueC>
```

项目经理层收到这三个 issue 后，
不会自己去做审计。
它会确认三件事：
每个 issue 是否足够独立、
每个 worker 是否已正确生成、
每个 worker 是否都绑定了独立分支与 worktree。

随后三个 worker 开始并行执行。

- Worker A 只看仓位与风险暴露。
- Worker B 只看数据质量与回测输入。
- Worker C 只看执行链路与成交假设。

它们分别在自己的 worktree 中阅读仓库、
产出修改或文档、
提交 commit、
推送分支、
打开 PR。
三条链彼此隔离，
因此不会互相污染上下文或工作目录。

当三个 PR 都出来后，
CEO 再进入审核阶段。
这时 CEO 的工作不是重做审计，
而是回答几个更高层的问题：

- 三个 PR 是否真的覆盖了用户最关心的三个风险面。
- 结论之间是否重复，是否有关键遗漏。
- 哪些风险证据已经足够强，哪些还需要补充。
- 是否需要再加一个总览 issue 或总览说明。

假设其中一个 PR 的 CI 失败，
reaction engine 会把失败详情送回原 worker，
而不是让 CEO 亲自去复现测试日志。
假设 reviewer 评论说：
“你提到的数据泄漏风险证据不足，请补一段复现实验说明。”
这条评论也会回到对应 worker，
由它在原 worktree 中继续补强。

最终 CEO 向用户输出的内容可以非常简洁：
三个风险面已分别完成审计，
其中仓位风险和执行假设风险的结论已经成熟，
数据质量风险的 PR 正在补充证据，
建议先审前两项。

这就是三层模型的价值。
CEO 始终保留在高杠杆位置，
项目经理维持执行秩序，
worker 则在隔离环境中把脏活累活做完。

## 角色边界与质量要求

这套模型要稳定运转，
必须把“边界清楚”看得和“交付完成”一样重要。
没有边界的并行，
最后只会变成更难追踪的混乱。

三层各自的质量要求可以概括如下：

- CEO：brief 要具体，派发要克制，审核要严格，汇报要简洁。
- 项目经理：会话要常驻，命名要稳定，映射要清楚，回流要可靠。
- Worker：issue 要单一，worktree 要隔离，提交要清晰，PR 要可审。

三层共同遵守一个原则：
不替上层做判断，
不替下层做执行。

上层替下层执行，
会浪费认知预算。
下层替上层判断，
会引入战略漂移。
这两个问题一旦同时出现，
系统很快就会退化成“所有人都做一点，但没人真正负责”。

## 常见误区

误区一：“CEO 看几眼代码没关系。”
如果只是为了审核结果是否合理，可以。
如果目的是推进实现，那就已经越界。

误区二：“orchestrator 既然最清楚当前状态，不如顺手改掉。”
不行。
编排层一旦直接写实现，就会同时承担调度和执行，
责任链立刻变模糊。

误区三：“一个 worker 顺便把另一个小 issue 也做了。”
这会破坏一 issue 一 worker 一 PR 的映射，
短期省事，长期极脏。

误区四：“CI 挂了先让 CEO 看看。”
通常没必要。
先让 reaction engine 回派给原 worker，
只有重试打满或方向错了，CEO 才需要介入。

误区五：“所有任务都值得走 AO。”
不对。
当流程成本高于执行成本时，
AO 反而是在制造额外负担。

## 十步速查表

下面这一页可以当作 CEO 的 dispatch cheat sheet。
如果你在紧张环境下需要快速判断下一步，
直接照着这十条做即可。

1. 先判断任务是单点处理，还是值得拆成多个并行子任务。
2. 如果值得并行，先拆子任务，不要急着开工。
3. 每个子任务写一份 brief，一份 brief 只服务一个 issue。
4. 用 `gh issue create --body-file` 创建 issue，让任务先获得编号和归属。
5. 用 `ao status` 检查 orchestrator 是否在线。
6. 如果 AO 没启动，进入项目目录执行 `ao start`。
7. 用 `ao batch-spawn <id1> <id2> ...` 一次生成对应 workers。
8. 定期查看 `ao status` 或 dashboard，但不要下场替 worker 做实现。
9. PR 出来后由 CEO 审核，再向用户总结哪些可合、哪些待修、风险是什么。
10. CI 失败或 review 到来时，优先相信 reaction engine 回派给原 worker；只有重试耗尽或方向错误时，CEO 才升级处理。

如果只能记住一句话，
就记住这一句：
CEO 负责想清楚、说清楚、查清楚；
项目经理负责派清楚、盯清楚；
worker 负责做清楚、交清楚。

只要这句话没有丢，
三层模型通常就不会塌。
