diff --git a/packages/core/src/__tests__/agent-workspace-hooks.test.ts b/packages/core/src/__tests__/agent-workspace-hooks.test.ts
index a953b0d3..f536123e 100644
--- a/packages/core/src/__tests__/agent-workspace-hooks.test.ts
+++ b/packages/core/src/__tests__/agent-workspace-hooks.test.ts
@@ -48,6 +48,20 @@ describe("buildAgentPath", () => {
     expect(aoIdx).toBe(0);
     expect(ghIdx).toBe(1);
   });
+
+  it("drops WSL-translated Windows PATH entries", () => {
+    const result = buildAgentPath(
+      "/mnt/c/Users/Administrator/bin:/usr/local/bin:/usr/bin:/mnt/d/Program Files/Git/usr/bin:/bin",
+    );
+    expect(result).toBe("/home/testuser/.ao/bin:/usr/local/bin:/usr/bin:/bin");
+    expect(result).not.toContain("/mnt/c/");
+    expect(result).not.toContain("/mnt/d/");
+  });
+
+  it("keeps non-drive /mnt paths", () => {
+    const result = buildAgentPath("/mnt/wslg/runtime-dir:/usr/bin:/bin");
+    expect(result).toBe("/home/testuser/.ao/bin:/usr/local/bin:/mnt/wslg/runtime-dir:/usr/bin:/bin");
+  });
 });
 
 describe("setupPathWrapperWorkspace", () => {
diff --git a/packages/core/src/agent-workspace-hooks.ts b/packages/core/src/agent-workspace-hooks.ts
index c1f0bb47..6c63bc46 100644
--- a/packages/core/src/agent-workspace-hooks.ts
+++ b/packages/core/src/agent-workspace-hooks.ts
@@ -18,6 +18,7 @@ import { randomBytes } from "node:crypto";
 
 const DEFAULT_PATH = "/usr/bin:/bin";
 const PREFERRED_GH_BIN_DIR = "/usr/local/bin";
+const WSL_WINDOWS_PATH_ENTRY = /^\/mnt\/[a-z](?:\/|$)/i;
 
 /** Preferred gh binary path for wrapper scripts */
 export const PREFERRED_GH_PATH = `${PREFERRED_GH_BIN_DIR}/gh`;
@@ -40,10 +41,14 @@ const WRAPPER_VERSION = "0.2.0";
 
 /**
  * Build a PATH string with ~/.ao/bin prepended for wrapper interception.
- * Deduplicates entries and ensures /usr/local/bin is early for gh resolution.
+ * Deduplicates entries, strips WSL-translated Windows entries, and ensures
+ * /usr/local/bin is early for gh resolution.
  */
 export function buildAgentPath(basePath: string | undefined): string {
-  const inherited = (basePath ?? DEFAULT_PATH).split(":").filter(Boolean);
+  const inherited = (basePath ?? DEFAULT_PATH)
+    .split(":")
+    .filter(Boolean)
+    .filter((entry) => !WSL_WINDOWS_PATH_ENTRY.test(entry));
   const ordered: string[] = [];
   const seen = new Set<string>();
 
diff --git a/packages/plugins/agent-codex/src/index.test.ts b/packages/plugins/agent-codex/src/index.test.ts
index 25a9d37b..e5eddc87 100644
--- a/packages/plugins/agent-codex/src/index.test.ts
+++ b/packages/plugins/agent-codex/src/index.test.ts
@@ -405,6 +405,18 @@ describe("getEnvironment", () => {
       process.env["PATH"] = originalPath;
     }
   });
+
+  it("strips WSL-translated Windows PATH entries from agent env", () => {
+    const originalPath = process.env["PATH"];
+    process.env["PATH"] =
+      "/mnt/c/Windows/system32:/usr/local/bin:/usr/bin:/mnt/d/Program Files/Git/usr/bin:/bin";
+    try {
+      const env = agent.getEnvironment(makeLaunchConfig());
+      expect(env["PATH"]).toBe("/mock/home/.ao/bin:/usr/local/bin:/usr/bin:/bin");
+    } finally {
+      process.env["PATH"] = originalPath;
+    }
+  });
  
   it("sets CODEX_DISABLE_UPDATE_CHECK=1 to suppress interactive update prompts", () => {
     const env = agent.getEnvironment(makeLaunchConfig());
