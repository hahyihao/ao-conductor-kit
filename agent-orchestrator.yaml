port: 3000

defaults:
  runtime: tmux
  orchestrator:
    agent: codex
  worker:
    agent: codex
  workspace: worktree
  notifiers: []

projects:
  ao-kit:
    name: AO Conductor Kit
    repo: hahyihao/ao-conductor-kit
    path: /root/projects/ao-conductor-kit
    defaultBranch: main
    sessionPrefix: kit
